# Local config.yaml for testing on macOS
# Package handles its own caching internally
output_image: today.png
vcom: -1.18
shutdown_after_display: false
log_file: logs/display.log

# Web server configuration
web_server:
  enabled: true
  host: "0.0.0.0"
  port: 8080
  debug: false 